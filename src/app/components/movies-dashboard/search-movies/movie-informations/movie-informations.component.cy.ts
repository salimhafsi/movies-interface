import { MovieInformationsComponent } from './movie-informations.component'

describe('MovieInformationsComponent', () => {
  it('should mount', () => {
    cy.mount(MovieInformationsComponent)
  })
})